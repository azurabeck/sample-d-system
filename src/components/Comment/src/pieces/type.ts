export interface CommentsProps {
  children?: React.ReactNode;
}
export interface CommentRootProps {
  userName: string;
  children?: React.ReactNode;
  color: string;
}
export interface CommetStyleProps {
  light?: boolean;
  color?: string;
}
